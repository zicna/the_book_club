<%= link_to "Create new Category", new_category_path if is_admin? %>
<div>
    <% @categories.each do |category| %>
        <p><%= link_to category.name, category_path(category) %></p>
    <% end %>
</div>